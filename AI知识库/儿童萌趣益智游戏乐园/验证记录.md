# 儿童萌趣益智游戏乐园 - 验证记录

标签：#验证记录 #测试 #构建 #低token

## 当前验证状态

- 积分防回归测试：已通过。
- 存储适配边界测试：已通过。
- 五子棋规则单元测试：已通过。
- 类型检查：已通过。
- 生产构建：已通过，无大 chunk 警告。
- 本地 dev server：首页 HTTP 200 已验证。
- 背景 BGM 文件资源：已通过，`/audio/Sunlight_on_the_Sandbox.mp3` 返回 200 `audio/mpeg`。
- HBuilder X 安卓 APK 真机验证：已通过，2026-06-10 用户反馈真机运行正常。
- 浏览器积分交互回归：已通过。
- 四个游戏基础通关浏览器回归：已通过。
- 首页快捷入口浏览器回归：已通过。
- 音频控制浏览器回归：已通过，背景音乐和音效开关独立。
- 模板残留搜索：已通过。
- 首屏 UI detector：已通过，`App.tsx` 无 impeccable detector 命中。
- 四个游戏内页 UI detector：已通过，`BombGame.tsx`、`KlotskiGame.tsx`、`SchulteGame.tsx`、`GomokuGame.tsx` 无 impeccable detector 命中。
- 四个游戏移动端视觉抽检：已通过，Playwright 截图检查数字炸弹、数字华容道、舒尔特训练、五子棋入口页。
- 全量 UI detector：已通过，`impeccable detect --json src` 当前无命中。
- 个人档案/荣誉墙移动端视觉抽检：已通过，Playwright 截图检查顶部档案、记录卡和荣誉墙卡片。
- 方案 B 绘本视觉抽检：已通过，Playwright 截图检查移动端首页绘本场景、游戏章节列表和四个游戏内页章节场景条。
- 奖励弹窗绘本化布局抽检：已通过，Playwright 检查移动端签到奖励弹窗宽 384、高约 422，按钮完整可见。
- 更完整的分支结算测试：部分未验证，五子棋平局、五子棋 AI 分支更精细测试仍待补充；数字炸弹 AI 胜利、五子棋亲子黑/白胜利、成就领奖防重复已通过浏览器回归。
- 小程序环境测试：未开始。

历史命令和完整输出见：`验证归档.md`。

## 常用验证顺序

```powershell
cd "D:\儿童微信小程序开发\儿童萌趣益智游戏乐园"
npm install
npm run test:points
npm run test:storage
npm run test:data-service
npm run test:gomoku
npm run test:browser:points
npm run test:browser:games
npm run test:browser:shortcuts
npm run test:browser:audio
npm run lint
npm run build
```

如果只改知识库文档，不需要运行 APP 测试；只需做 Git 差异和 Markdown 内容检查。

## 启动本地 Web 原型

```powershell
cd "D:\儿童微信小程序开发\儿童萌趣益智游戏乐园"
npm run dev
```

默认地址：

```text
http://localhost:3000
```

## 已验证事实

- 四个游戏组件不再直接写入 `profile.points`。
- 积分奖励统一通过 `App.tsx` 的 `handlePointsChange` 入口。
- 每日打卡积分变化：`+20`。
- 购买第一个零食糖果积分变化：`-15`。
- AI Studio/Gemini/Express/Dotenv 模板残留搜索无命中。
- 构建拆包后主入口 JS 约 156.28 kB，`vendor-icons` 约 15.95 kB。
- 背景 BGM 使用 `public/audio/Sunlight_on_the_Sandbox.mp3`，生产构建会输出到 `dist/audio/Sunlight_on_the_Sandbox.mp3`。
- HBuilder X 安卓 APK 已在真机通过，当前可作为 v0.1.0 安卓内测基线。
- `#music-mute-toggle-btn` 和 `#sound-mute-toggle-btn` 分别写入 `kids_applet_music_muted`、`kids_applet_effects_muted`，两个开关互不联动。
- APP 源码里的浏览器存储访问已收口到 `src/utils/gameStorage.ts`；`npm run test:storage` 会阻止组件和业务工具重新直接调用 `localStorage` / `sessionStorage`。
- `src/utils/gameDataService.ts` 已升级为异步 `GameDataPort` 边界；`npm run test:data-service` 会检查服务层和排行榜边界。
- `src/utils/cloudGameDataPort.ts` 已新增云端 port 草案但未启用；`npm run test:data-service` 会检查云函数名存在、默认仍使用本地 port，并用 mock 调用器验证 `loginOrCreateUser -> getHomeState`、`saveGameState`、`getLeaderboard` 行为。
- `cloudfunctions/` 已新增读取链路骨架；`loginOrCreateUser`、`getHomeState`、`getLeaderboard` 均已有本地可测试实现草案，`npm run test:data-service` 会检查目录文档与前端云函数名对齐。
- 首屏 UI 已通过 `impeccable detect` 扫描，`src/App.tsx` 当前无命中。
- Playwright 截图已检查移动端首页、游戏入口、排行榜和桌面首页。
- 游戏内页 UI 已通过 `impeccable detect` 扫描，四个游戏组件当前无命中。
- `DESIGN.md` 已加入项目根目录，记录配色、字体、组件、动效和禁用模式。
- Playwright 截图已检查四个游戏移动端入口页，临时截图未保留。
- 个人档案、荣誉墙、签到弹窗已完成外围 UI 降噪，全 `src` 当前无 impeccable detector 命中。
- Playwright 截图已检查移动端个人档案和荣誉墙，临时截图未保留。
- 首页、游戏入口和四个游戏内页已改为方案 B 绘本探索乐园方向，全 `src` 当前无 impeccable detector 命中。
- Playwright 截图已检查移动端首页、游戏章节页和四个游戏内页章节场景条，临时截图未保留。
- `npm run test:browser:games` 已覆盖四个游戏基础通关和主要分支路径：数字炸弹惩罚鼓励 `+10`、数字炸弹 AI 踩雷胜利 `+50`、数字华容道通关 `+80`、舒尔特通关 `+60`、五子棋亲子模式黑子胜利 `+50`、五子棋亲子模式白子胜利 `+50`、成就领奖首领 `+50` 且第二次不重复加分。
- `npm run test:gomoku` 已覆盖五子棋横/竖/斜五连、四连不误判、满盘无五连平局；`npm run test:browser:games` 已覆盖五子棋 AI 模式玩家胜利 `+100` 和游戏结束后点击不重复加分。
- `npm run test:browser:shortcuts` 已覆盖首页三枚绘本印章快捷入口：背包补给、今日路线、成长印章均可跳到对应区域。
- `#reward-claimed-modal` 和 `#achievement-alert-root` 已保留，奖励弹窗视觉改造未破坏现有关闭弹窗与浏览器测试入口。

## 待验证清单

### 积分和玩法

- [x] 每日打卡只加一次积分。
- [x] 零食购买只扣一次积分。
- [x] 数字炸弹胜利只加一次积分。
- [x] 数字炸弹惩罚鼓励只加一次积分。
- [x] 数字华容道通关只加一次积分。
- [x] 舒尔特训练通关只加一次积分。
- [x] 五子棋胜利、失败、平局只结算一次积分；亲子模式黑子/白子胜利、AI 模式玩家胜利、防结束后重复加分已自动验证，平局规则已由单元测试覆盖。
- [x] 成就领奖只发一次奖励。
- [ ] 排行榜同步当前用户积分。

### 小程序迁移

- [ ] 存储 API 替换后能读写用户档案。
- [ ] 签到日期按 Asia/Shanghai 预期工作。
- [ ] 音效在微信开发者工具中可播放或优雅降级。
- [ ] 图片上传和头像裁剪可用。
- [ ] 四个游戏触控操作在真机上可用。

## 2026-06-10 追加：loginOrCreateUser 验证

- `npm.cmd run test:data-service` 已通过。
- 本次新增覆盖：`loginOrCreateUser` 新用户默认状态初始化、老用户登录时间刷新、缺少可信微信 `_openid` 时抛出 `UNAUTHENTICATED`。
- `loginOrCreateUser` 仍未做微信云环境部署验证，下一步需要在真实云函数中确认 `cloud.getWXContext().OPENID` 可用。

## 2026-06-10 追加：getHomeState 验证

- `npm.cmd run test:data-service` 已通过。
- 本次新增覆盖：`getHomeState` 首页 `GameState` 映射、排行榜当前用户兜底、缺少可信微信 `_openid` 时抛出 `UNAUTHENTICATED`、缺少用户时抛出 `USER_NOT_FOUND`。
- `getHomeState` 仍未做微信云环境部署验证，下一步需要在真实云数据库集合中确认字段名与索引。

## 2026-06-10 追加：getLeaderboard 验证

- `npm.cmd run test:data-service` 已通过。
- 本次新增覆盖：`getLeaderboard` 按服务端积分排序、当前用户标记、忽略客户端传入分数、limit 服务端归一化、缺少可信微信 `_openid` 时抛出 `UNAUTHENTICATED`、缺少用户时抛出 `USER_NOT_FOUND`。
- `getLeaderboard` 仍未做微信云环境部署验证，下一步需要在真实云数据库中确认 `user_stats.pointsBalance` 排序索引和 `users` 批量查询。

## 2026-06-10 追加：saveGameState 验证

- `npm.cmd run test:data-service` 已通过。
- 本次新增覆盖：`saveGameState` 白名单保存用户档案/积分/记录/成就/签到/背包、拒绝 `profile.id` 与当前 `_openid` 不匹配、缺少可信微信 `_openid` 时抛出 `UNAUTHENTICATED`、缺少用户时抛出 `USER_NOT_FOUND`、不写入客户端排行榜。
- `saveGameState` 仍未做微信云环境部署验证，下一步需要在真实云数据库中确认批量替换成就和背包的性能与权限规则。

## 2026-06-10 追加：云函数部署包结构验证

- `npm.cmd run test:cloudfunctions` 已通过。
- `npm.cmd run test:data-service` 已通过。
- 本次新增覆盖：四个云函数目录必须同时存在 `README.md`、`index.cjs`、`package.json`，且 `package.json` 必须声明 `main: "index.cjs"` 和 `wx-server-sdk: "latest"`。
- 当前仍未真实部署到微信云开发环境。

## 2026-06-10 追加：前端云端切换开关验证

- `npm.cmd run test:data-service` 已通过。
- `npm.cmd run lint` 已通过。
- `npm.cmd run verify:android` 已通过。
- 本次新增覆盖：默认仍走本地 port；`wx.cloud` 不存在时即使开关开启也回退本地；只有显式开启且 `wx.cloud.callFunction` 存在时才走云端 port。
## 2026-06-10 追加：云端开关防误开验证

- `npm.cmd run test:data-service` 已加入 `.env.example` 和 `微信云开发部署前清单.md` 的静态检查。
- `npm.cmd run test:data-service` 已通过。
- `npm.cmd run lint` 已通过。
- `npm.cmd run verify:android` 已通过。
- 当前预期：`.env.example` 必须保持 `VITE_USE_WECHAT_CLOUD=false`；真实部署和冒烟通过前，不允许把示例配置改成 `VITE_USE_WECHAT_CLOUD=true`。
## 2026-06-10 追加：loginOrCreateUser 部署准备护栏验证

- 新增 `tests/cloudfunctions-deploy-readiness.test.mjs`，检查 `loginOrCreateUser` 部署文档和云端开关禁用约束。
- `npm.cmd run test:cloudfunctions` 已通过。
- `npm.cmd run test:data-service` 已通过。
- `npm.cmd run lint` 已通过。
- `npm.cmd run verify:android` 已通过。

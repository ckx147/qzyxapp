# 儿童萌趣益智游戏乐园 - 问题与风险台账

标签：#风险台账 #bug #验证状态

## 状态说明

- 未处理：已发现，还没动手。
- 处理中：已有修改但未验证。
- 已修复：代码已改。
- 已验证：代码已改且通过检查。
- 暂缓：明确以后再处理。

## P0

| ID | 问题 | 状态 | 影响 | 位置 | 建议 |
| --- | --- | --- | --- | --- | --- |
| RISK-001 | 积分重复发放 | 已验证 | 积分、排行榜、商店经济、成就反馈失真 | `App.tsx`、四个游戏组件 | 已统一通过 `handlePointsChange` 改积分；新增 `npm run test:points` 防回归 |

## P1

| ID | 问题 | 状态 | 影响 | 位置 | 建议 |
| --- | --- | --- | --- | --- | --- |
| RISK-002 | 不是微信小程序工程 | 未处理 | 不能直接提交小程序运行 | 全项目 | 先决定 WebView、Taro/uni-app 或原生小程序 |
| RISK-003 | 使用 Web API | 未处理 | 小程序迁移需要替换 API | `localStorage`、`window`、`document`、`AudioContext` 等 | 建 API 适配层或按目标框架重构 |
| RISK-004 | 本地数据无法跨设备 | 未处理 | 用户数据、成就、打卡、排行榜会丢失 | `gameHelpers.ts` | 引入微信登录和云端数据 |
| RISK-005 | 构建未验证 | 已验证 | 无法确认当前代码能否构建上线 | 项目根目录 | `npm run lint` 和 `npm run build` 已通过；构建有大 chunk 警告 |

## P2

| ID | 问题 | 状态 | 影响 | 位置 | 建议 |
| --- | --- | --- | --- | --- | --- |
| RISK-006 | AI Studio 模板残留 | 已验证 | 项目信息不专业，可能误导部署 | `package.json`、`README.md`, `.env.example`, `metadata.json`, `index.html`, `vite.config.ts` | 已清理模板文案、Gemini 能力声明和未使用依赖 |
| RISK-007 | 本地假排行榜 | 未处理 | 不是真实社交或全服排行 | `gameHelpers.ts` | 上线前改云端排行榜 |
| RISK-008 | 儿童隐私和家长授权未评估 | 未处理 | 上线合规风险 | 产品层 | 小程序上线前补隐私政策和授权流程 |

## 验证记录

### 2026-06-08

- 尝试运行：`npm run lint`
- 结果：失败
- 原因：`tsc` 不可用，推断依赖未安装或环境未准备好。

### 2026-06-09

- 修复：积分重复发放。
- 验证：`npm run test:points`、`npm run lint`、`npm run build` 均通过。
- 备注：`vite build` 仍提示 JS chunk 大于 500 kB，属于性能优化项，不阻塞当前修复。

### 2026-06-09

- 修复：AI Studio 模板残留。
- 验证：残留搜索无命中；`npm run test:points`、`npm run test:browser:points`、`npm run lint`、`npm run build` 均通过。
- 备注：`vite build` 仍提示 JS chunk 大于 500 kB。

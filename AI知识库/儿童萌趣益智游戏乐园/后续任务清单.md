# 儿童萌趣益智游戏乐园 - 后续任务清单

标签：#任务清单 #儿童小程序 #迁移计划

## 近期优先级

- [x] 修复积分重复发放问题。
- [x] 安装依赖并运行 `npm run lint`。
- [x] 运行 `npm run build`，确认当前 Web 原型可构建。
- [x] 清理 `package.json`、`README.md`、`.env.example`、`metadata.json` 中的 AI Studio 模板残留。
- [x] 消除 Vite 构建大 chunk 警告。
- [x] 建立云端数据模型草案。
- [x] 去除知识库高频入口的过期状态，并新增任务启动卡。
- [x] 建立小程序路线决策矩阵。
- [x] 建立微信云开发落地清单。
- [x] 建立儿童隐私合规清单。
- [x] 建立四个游戏手动测试脚本。
- [x] 建立四个游戏基础通关浏览器回归脚本。
- [x] 建立 v0.1.0 安卓内测发包基线。
- [ ] 用户确认小程序路线：当前建议 Taro/uni-app MVP。
- [ ] 基于路线创建迁移实施任务清单。

## 积分系统修复思路

- [x] 统一约定：只有 `App.tsx` 的 `handlePointsChange` 能修改 `profile.points`。
- [x] 游戏组件结算时删除直接 `points: prev.points + ...`。
- [x] 五子棋不要同时更新 `updatedPoints` 和调用 `onPointsChange(diff, ...)`。
- [ ] 修复后手动验证每个奖励入口：
  - [x] 每日打卡
  - [x] 零食购买扣分
  - [x] 数字炸弹胜利
  - [x] 数字炸弹惩罚鼓励
  - [x] 华容道通关
  - [x] 舒尔特通关
  - [ ] 五子棋胜、负、平；亲子黑/白胜利已验证，AI 分支和平局待补充
  - [x] 成就领奖

## 已新增自动验证

- [x] `npm run test:points`：静态防止游戏组件直接写 `profile.points`。
- [x] `npm run test:browser:points`：浏览器回归打卡 `+20` 和购买糖果 `-15`。
- [x] `npm run test:browser:games`：浏览器回归数字炸弹惩罚鼓励、数字炸弹 AI 踩雷胜利、数字华容道通关、舒尔特通关、五子棋亲子模式黑子/白子胜利、成就领奖防重复。

## 小程序迁移待评估

- [ ] `localStorage` 替换为 `wx.getStorageSync` / `wx.setStorageSync` 或云端存储。
- [ ] `AudioContext` 替换为小程序音频方案。
- [ ] 图片上传和裁剪逻辑替换为小程序文件与 Canvas API。
- [ ] React DOM 页面改造为目标框架页面。
- [ ] 排行榜从本地假数据改为云端数据。
- [ ] 儿童产品隐私与家长授权流程评估。

## 游戏手动验证待执行

- [x] 按 `四个游戏测试脚本.md` 验证数字炸弹 AI 胜利路径。
- [x] 按 `四个游戏测试脚本.md` 验证数字炸弹惩罚鼓励路径。
- [x] 按 `四个游戏测试脚本.md` 验证数字华容道通关。
- [x] 按 `四个游戏测试脚本.md` 验证舒尔特训练通关。
- [ ] 按 `四个游戏测试脚本.md` 验证五子棋 AI 胜、平局。
- [x] 按 `四个游戏测试脚本.md` 验证五子棋亲子模式黑子胜利。
- [x] 按 `四个游戏测试脚本.md` 验证五子棋亲子模式白子胜利。
- [x] 按 `四个游戏测试脚本.md` 验证成就领奖不能重复领取。

## 微信云开发待落地

- [ ] 确认使用微信云开发还是自有后端。
- [x] 将 `gameDataService.ts` 升级为异步 `GameDataPort` 边界，保留本地实现，方便后续替换云端 port。
- [x] 新增 `cloudGameDataPort.ts` 草案，预留 `loginOrCreateUser`、`getHomeState`、`saveGameState`、`getLeaderboard` 调用位置但不启用。
- [x] 建立 `云函数接口契约.md`，记录云函数请求、响应、mock、错误码和前端接入顺序。
- [x] 新增 `cloud-game-data-port.test.mjs`，用 mock 调用器验证云端 port 调用顺序和排行榜返回值。
- [x] 建立 `cloudfunctions/` 读取链路骨架，包含 `loginOrCreateUser`、`getHomeState`、`getLeaderboard` README。
- [ ] 创建 `users`、`user_stats`、`point_transactions`、`achievements`、`checkins`、`inventory_items` 集合。
- [ ] 部署/接通 `loginOrCreateUser` 和 `getHomeState`。
- [ ] 实现 `claimDailyCheckin`、`purchaseFood`、`feedCompanion`、`claimAchievementReward`。
- [ ] 实现 `submitGameResult`，先接入一个小游戏。
- [ ] 实现 `getLeaderboard`。

## 2026-06-10 追加：微信云开发待落地

- [x] 新增 `loginOrCreateUser` 实现草案，并用本地单元测试覆盖核心逻辑。
- [x] 新增 `getHomeState` 实现草案，并用本地单元测试覆盖首页读取链路。
- [ ] 在微信云开发环境创建集合：`users`、`user_stats`、`point_transactions`、`achievements`、`checkins`、`inventory_items`。
- [ ] 部署 `loginOrCreateUser` 云函数，并记录 `_openid` 真机/开发者工具验证结果。
- [ ] 部署 `getHomeState` 云函数，并验证真实云数据库返回前端 `GameState` 形状。

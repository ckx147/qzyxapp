# 儿童萌趣益智游戏乐园 - 文件地图

标签：#文件地图 #低token #源码入口

APP 根目录：`D:\儿童微信小程序开发\儿童萌趣益智游戏乐园`

## 默认入口

- `src/App.tsx`：主应用、导航、全局状态、积分入口、成就弹窗。
- `src/main.tsx`：React 挂载入口。
- `src/index.css`：全局样式、Tailwind、自定义动画和背景。

## 数据、成就、排行榜

必读：

- `src/utils/gameHelpers.ts`
- `src/utils/gameDataService.ts`
- `src/utils/cloudGameDataPort.ts`
- `src/utils/leaderboardService.ts`
- `src/utils/tierConfig.ts`
- `src/App.tsx`
- `cloudfunctions/README.md`
- `cloudfunctions/loginOrCreateUser/README.md`
- `cloudfunctions/getHomeState/README.md`
- `cloudfunctions/getLeaderboard/README.md`

按需：

- `src/components/AchievementsView.tsx`
- `src/components/ProfileView.tsx`
- `本地与云端数据边界.md`
- `云函数接口契约.md`

## 积分系统

必读：

- `src/App.tsx`
- `src/components/BombGame.tsx`
- `src/components/KlotskiGame.tsx`
- `src/components/SchulteGame.tsx`
- `src/components/GomokuGame.tsx`

可选：

- `src/components/DailyCheckIn.tsx`
- `src/components/CompanionInteractions.tsx`
- `src/components/AchievementsView.tsx`

搜索建议：

```powershell
rg -n "onPointsChange|points: prev\.points|updatedPoints|profile\.points" src
```

## 打卡、宠物、商店

必读：

- `src/components/DailyCheckIn.tsx`
- `src/components/CompanionInteractions.tsx`
- `src/utils/gameHelpers.ts`
- `src/App.tsx`

## 四个小游戏

- 数字炸弹：`src/components/BombGame.tsx`
- 数字华容道：`src/components/KlotskiGame.tsx`
- 舒尔特训练：`src/components/SchulteGame.tsx`
- 五子棋：`src/components/GomokuGame.tsx`

## UI 和体验

必读：

- `src/App.tsx`
- `src/index.css`
- 当前任务相关组件

## 音效

必读：

- `src/utils/audio.ts`
- `src/App.tsx`

小程序迁移时重点看 `AudioContext`。

## 配置与模板残留

必读：

- `package.json`
- `README.md`
- `.env.example`
- `metadata.json`
- `vite.config.ts`
- `HBuilderX打包白屏排查.md`
- `安卓打包清单.md`
- `v0.1.0安卓内测发包基线.md`
- `public/branding/*`

当前状态：

- `package.json` 包名已改为 `kids-brain-game-park`。
- `README.md` 已改为项目说明。
- `.env.example` 和 `metadata.json` 的 Gemini/AI Studio 模板痕迹已清理。
- 未使用的 `@google/genai`、`express`、`dotenv`、`@types/express` 已卸载。

## 小程序迁移扫描

建议先跑：

```powershell
rg -n "localStorage|window\.|document\.|new Image|AudioContext|ReactDOM|createRoot|File|canvas|URL\.createObjectURL" src
```

再按搜索结果读局部代码。

## 2026-06-10 追加：loginOrCreateUser 云函数草案

必读：
- `cloudfunctions/loginOrCreateUser/index.cjs`：当前 `loginOrCreateUser` 云函数实现草案，包含纯函数核心与微信云函数 `main` 入口。
- `tests/login-or-create-user-cloudfunction.test.mjs`：云函数核心逻辑单元测试。

相关：
- `cloudfunctions/loginOrCreateUser/README.md`：职责、集合、响应形状和部署前注意事项。
- `tests/cloudfunctions-skeleton.test.mjs`：检查 `loginOrCreateUser` 已从骨架升级为实现草案，其余读路径云函数仍保持 README 骨架。

## 2026-06-10 追加：getHomeState 云函数草案

必读：
- `cloudfunctions/getHomeState/index.cjs`：当前 `getHomeState` 云函数实现草案，负责组装前端 `GameState`。
- `tests/get-home-state-cloudfunction.test.mjs`：首页读取链路单元测试。

相关：
- `cloudfunctions/getHomeState/README.md`：职责、集合、响应形状和部署前注意事项。
- `cloudfunctions/loginOrCreateUser/index.cjs`：`getHomeState` 假设用户和默认记录已由登录函数创建。

## 2026-06-10 追加：getLeaderboard 云函数草案

必读：
- `cloudfunctions/getLeaderboard/index.cjs`：当前 `getLeaderboard` 云函数实现草案，负责服务端权威排行榜读取。
- `tests/get-leaderboard-cloudfunction.test.mjs`：排行榜云函数单元测试。

相关：
- `cloudfunctions/getLeaderboard/README.md`：职责、集合、响应形状和部署前注意事项。
- `src/utils/cloudGameDataPort.ts`：前端云端 port 会调用 `getLeaderboard`，但该函数不信任客户端分数。

## 2026-06-10 追加：saveGameState 云函数草案

必读：
- `cloudfunctions/saveGameState/index.cjs`：当前 `saveGameState` 云函数实现草案，负责白名单保存前端 `GameState`。
- `tests/save-game-state-cloudfunction.test.mjs`：保存链路安全边界单元测试。

相关：
- `cloudfunctions/saveGameState/README.md`：职责、集合、响应形状和部署前注意事项。
- `src/utils/cloudGameDataPort.ts`：前端云端 port 会把完整 `GameState` 传给 `saveGameState`。
- `cloudfunctions/getLeaderboard/index.cjs`：排行榜不由 `saveGameState` 写入，而由该函数按服务端积分计算。

## 2026-06-10 追加：微信云开发部署

必读：
- `微信云开发部署前清单.md`：真实部署前的集合、索引、权限、部署顺序和冒烟测试。
- `微信云开发落地清单.md`：更完整的云开发规划和后续云函数路线。

相关：
- `数据模型草案.md`：集合字段来源和长期数据模型。
- `云函数接口契约.md`：云函数请求/响应和错误码约定。

## 2026-06-10 追加：云函数部署包结构

必读：
- `cloudfunctions/loginOrCreateUser/package.json`
- `cloudfunctions/getHomeState/package.json`
- `cloudfunctions/saveGameState/package.json`
- `cloudfunctions/getLeaderboard/package.json`
- `tests/cloudfunctions-skeleton.test.mjs`

说明：
- 四个云函数包都以 `index.cjs` 为入口。
- 四个云函数包都显式依赖 `wx-server-sdk`。

## 2026-06-10 追加：前端云端切换开关

必读：
- `src/utils/gameDataService.ts`：`resolveGameDataPort()` 根据开关和微信环境选择本地或云端 port。
- `src/utils/wechatCloudAdapter.ts`：封装 `wx.cloud.callFunction`。
- `tests/wechat-cloud-adapter.test.mjs`：微信云调用适配器测试。
- `tests/game-data-port-selection.test.mjs`：数据 port 选择测试。

说明：
- 默认仍使用本地数据。
- `VITE_USE_WECHAT_CLOUD=true` 只是必要条件，还必须存在 `wx.cloud.callFunction`。
- 真正切云前先部署云函数并完成 `微信云开发部署前清单.md` 的冒烟测试。
## 2026-06-10 追加：云端开关防误开

- `.env.example`：示例环境变量；`VITE_USE_WECHAT_CLOUD=false` 必须保持为默认值，真实云函数部署和冒烟通过后才在实际环境里开启。
- `tests/data-service-boundary.test.mjs`：数据服务边界静态测试，同时检查云端开关默认关闭和部署清单记录。
## 2026-06-10 追加：微信云开发真实接入操作闭环

- `微信云开发真实接入操作闭环.md`：真实进入微信开发者工具/云开发控制台时使用的一页式操作手册，包含集合创建、云函数部署后验证、云端开关开启门槛、真机验证和回退方案。

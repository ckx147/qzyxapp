# 儿童萌趣益智游戏乐园 - 当前状态

标签：#当前状态 #低token #交接

更新时间：2026-06-09

## 一句话状态

当前可见 APP 品牌名已改为「小布智趣岛」，顶部等级副标题为「Lv.8 探索馆」；源码目录和知识库目录仍保留「儿童萌趣益智游戏乐园」以维持路径稳定。

当前是 Vite + React + TypeScript Web 原型，功能较完整，但还不是微信小程序工程；积分重复发放、AI Studio 模板残留、构建大 chunk 警告和主要游戏/奖励积分链路均已处理并通过自动验证；UI 已选择方案 B“绘本探索乐园”方向，首页、游戏入口、四个游戏内页、签到奖励弹窗和成就解锁弹窗已改为绘本章节/故事场景视觉，首页的背包补给、今日路线、成长印章已改成真实快捷入口，个人档案、荣誉墙和签到弹窗已按 taste/impeccable 做降噪和系统化优化；五子棋胜负/平局规则已抽为可测试工具，AI 胜利分支和结束后防重复加分已纳入浏览器回归；本地存储已收口到 `src/utils/gameStorage.ts`，为后续替换微信 `wx.getStorageSync/wx.setStorageSync` 或云开发做准备；已补充小程序路线决策矩阵、微信云开发落地清单、儿童隐私合规清单和四个游戏测试脚本，下一步可继续确认小程序路线，或做排行榜/存储云端化准备。

## 当前音频状态

- 背景 BGM 已改为用户提供的文件版音乐：`public/audio/Sunlight_on_the_Sandbox.mp3`，运行时通过 `/audio/Sunlight_on_the_Sandbox.mp3` 循环播放。
- 背景音乐和交互音效已拆成两个独立开关：`#music-mute-toggle-btn` 控制 BGM，`#sound-mute-toggle-btn` 控制音效。
- BGM 已加入淡入/淡出和播放失败兜底；点击、移动、得分、胜利、警告、爆炸等交互音效仍保留合成音效实现。

## 当前存储状态

- APP 源码中的浏览器存储访问已统一收口到 `src/utils/gameStorage.ts`。
- 游戏业务状态已新增服务层 `src/utils/gameDataService.ts`，暴露 `loadGameState()`、`saveGameState()`、`syncLeaderboard()`。
- 排行榜读写已抽到 `src/utils/leaderboardService.ts`，后续可替换为云数据库或云函数 `getLeaderboard`。
- 本地数据边界见 `本地与云端数据边界.md`：音频设置和临时 UI 设置留本地；用户档案、积分、成就、排行榜、签到等核心进度上云。

## 当前 HBuilder X / APK 状态

- 当前安卓内测版本为 v0.1.0，版本字段在 `package.json`。
- 2026-06-10 用户已确认 HBuilder X 打包后的安卓 APK 真机通过。
- 安卓 APK 白屏优先按 `HBuilderX打包白屏排查.md` 排查。
- 用户提供的 APP 图标和启动页已纳入 `public/branding`，HBuilder X 使用 `app-icon-1024.png`、`app-icon-512.png` 和 `splash-1080x1920.png`。
- `vite.config.ts` 已设置 `base: './'`，构建产物使用 `./assets/...` 相对路径。
- BGM 路径已改为基于 `import.meta.env.BASE_URL`，构建后为 `./audio/Sunlight_on_the_Sandbox.mp3`。
- 已新增 `npm run test:android-package`，打包前先运行 `npm.cmd run build`，再运行该检查，通过后再用 HBuilder X 选择 `dist` 目录打包。
- `index.html` 已新增内联启动诊断；若真机主脚本未加载、React 未挂载或资源加载失败，会显示 `app-boot-diagnostics` 面板辅助定位。
- 已新增 `npm run test:browser:dist`，用本地静态服务器加载生产 `dist` 并验证 React 挂载、诊断面板不出现、资源无 404。
- 已新增 `npm run verify:android`，一键执行 `build`、`test:android-package`、`test:browser:dist`，HBuilder X 打包前优先运行这一条。
- 已新增 `安卓打包清单.md`，记录 HBuilder X 入口目录、版本、图标、启动页、权限和发包记录模板。

## 当前不要做

- 不要默认全量扫描整个 `src`。
- 不要在未确认任务前重构项目结构。
- 不要直接迁移到小程序框架，除非用户明确要求。
- 不要改 APP 源码，除非用户明确进入实现或修复任务。

## 当前优先事项

1. 用户确认小程序路线：当前文档建议优先评估 Taro/uni-app MVP。
2. 基于 `微信云开发落地清单.md` 创建云开发集合和云函数任务。
3. 补充更细分的结算路径：五子棋平局、五子棋 AI 分支更精细测试、排行榜同步当前用户积分。
4. 按 `儿童隐私合规清单.md` 确认上线前合规边界。

## 知识库状态

- 已建立低 token 入口：`README.md`、`任务启动卡.md`、`当前状态.md`。
- 已新增更短的新窗口入口：`任务启动卡.md`。
- 已建立项目操作台：`项目速读.md`、`文件地图.md`、`问题与风险台账.md`、`决策记录.md`、`后续任务清单.md`。
- 已新增过程记录：`变更日志.md`、`验证记录.md`、`验证归档.md`、`小程序迁移方案.md`、`小程序路线决策矩阵.md`、`数据模型草案.md`、`微信云开发落地清单.md`、`儿童隐私合规清单.md`、`四个游戏测试脚本.md`。

## 已修复 P0

积分系统重复发放已修复：四个游戏组件不再直接修改 `profile.points`，统一通过 `App.tsx` 的 `handlePointsChange`。

涉及：

- `src/components/BombGame.tsx`
- `src/components/KlotskiGame.tsx`
- `src/components/SchulteGame.tsx`
- `src/components/GomokuGame.tsx`
- 统一入口：`src/App.tsx` 的 `handlePointsChange`

## 验证状态

- `npm install` 已完成，生成 `package-lock.json`。
- `npm run test:points` 已通过。
- `npm run test:storage` 已通过。
- `npm run test:gomoku` 已通过。
- `npm run test:browser:points` 已通过。
- `npm run test:browser:games` 已通过。
- `npm run test:browser:shortcuts` 已通过。
- `npm run test:browser:audio` 已通过。
- `npm run lint` 已通过。
- `npm run build` 已通过，大 chunk 警告已消失。
- 背景 BGM 资源 `/audio/Sunlight_on_the_Sandbox.mp3` 已验证返回 200 `audio/mpeg`，生产构建已生成 `dist/audio/Sunlight_on_the_Sandbox.mp3`。
- 本地 dev server 首页 HTTP 200 已验证，地址：`http://localhost:3000`。
- 浏览器积分交互回归已通过：打卡 `+20`，购买糖果 `-15`。
- AI Studio/Gemini/Express/Dotenv 模板残留搜索已无命中。
- 构建拆包已优化：主入口 JS 约 156.28 kB，`vendor-icons` 约 15.95 kB。
- UI 首屏优化已完成：不再首次进入就弹成就弹窗；游戏入口、排行榜、桌面说明卡已降低厚底边、渐变文字和过度动效；`App.tsx` impeccable detector 无命中。
- UI 游戏内页优化已完成：新增 `DESIGN.md`；数字炸弹、数字华容道、舒尔特训练、五子棋的外层容器、说明卡、结果卡和主按钮已统一为轻量儿童产品风格；四个游戏组件 impeccable detector 无命中；已用 Playwright 截图抽检移动端四个游戏入口页。
- UI 外围成长系统优化已完成：个人档案、荣誉墙、签到弹窗已移除厚底边框和 bounce 动效，接入共享轻量卡片体系；全 `src` impeccable detector 当前无命中；已用 Playwright 截图抽检移动端荣誉墙页面。
- 方案 B 绘本探索乐园已扩展到游戏内页：`DESIGN.md` 已记录 `storybook exploration park`；首页新增绘本场景和今日章节；游戏入口由 2x2 卡片改为四个故事章节列表；四个游戏内页新增章节场景条；已用 Playwright 截图抽检首页、游戏章节页和四个游戏入口页。
- 奖励弹窗绘本化已完成：签到奖励弹窗和成就解锁弹窗统一为绘本纸张、贴纸宝盒和场景山丘视觉，保留 `#reward-claimed-modal` 与 `#achievement-alert-root` 测试入口。
- 首页三枚绘本印章已改成真实快捷入口：背包补给滚动到背包/商店区域，今日路线进入游戏堡，成长印章进入荣誉墙；`npm run test:browser:shortcuts` 和 in-app browser 均已验证。
- 四个游戏基础通关与主要分支回归已完成：数字炸弹惩罚鼓励 `+10`、数字炸弹 AI 踩雷胜利 `+50`、数字华容道通关 `+80`、舒尔特通关 `+60`、五子棋亲子模式黑子胜利 `+50`、五子棋亲子模式白子胜利 `+50`、成就领奖首领 `+50` 且第二次不重复加分。
- 五子棋规则补强已完成：`src/utils/gomokuRules.ts` 覆盖横/竖/斜五连、四连不误判、满盘平局；浏览器回归覆盖 AI 模式玩家胜利 `+100` 和游戏结束后继续点击不重复加分。

## 新窗口建议

先读：

- `README.md`
- `任务启动卡.md`
- 本文件

然后按任务读：

- 找源码：`文件地图.md`
- 看风险：`问题与风险台账.md`
- 查验证：`验证记录.md`
- 看迁移路线：`小程序迁移方案.md`
- 做路线决策：`小程序路线决策矩阵.md`
- 看云端数据：`数据模型草案.md`
- 看云开发落地：`微信云开发落地清单.md`
- 看儿童隐私合规：`儿童隐私合规清单.md`
- 做游戏手动测试：`四个游戏测试脚本.md`
- 快速理解：`项目速读.md`
- 看完整背景：`项目概览与交接.md`

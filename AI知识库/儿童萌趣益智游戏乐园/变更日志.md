# 儿童萌趣益智游戏乐园 - 变更日志

标签：#变更日志 #项目记录

## 记录格式

- 日期：
- 范围：
- 变更：
- 验证：

## 2026-06-10

- 范围：微信云开发 / 云函数目录骨架
- 变更：新增 `cloudfunctions/` 规划骨架，包含 `loginOrCreateUser`、`getHomeState`、`getLeaderboard` 三个读取链路云函数 README；新增 `tests/cloudfunctions-skeleton.test.mjs` 检查目录文档存在并与前端 `cloudGameDataPort` 云函数名对齐；`test:data-service` 已纳入该检查；同步当前状态、文件地图、验证记录和后续任务清单。
- 验证：`npm.cmd run test:data-service`、`npm.cmd run lint`、`npm.cmd run verify:android` 均通过。

## 2026-06-10

- 范围：数据服务层 / 云端 port mock 测试
- 变更：新增 `tests/cloud-game-data-port.test.mjs`，用 mock `callCloudFunction` 验证 `cloudGameDataPort` 的读取顺序为 `loginOrCreateUser -> getHomeState`，保存会向 `saveGameState` 传完整状态，排行榜以 `getLeaderboard` 返回值为准；`test:data-service` 已纳入该测试；更新验证记录和后续任务清单。
- 验证：`npm.cmd run test:data-service`、`npm.cmd run lint`、`npm.cmd run verify:android` 均通过。

## 2026-06-10

- 范围：微信云开发 / 云函数接口契约
- 变更：新增 `云函数接口契约.md`，记录 `loginOrCreateUser`、`getHomeState`、`saveGameState`、`getLeaderboard` 的职责、请求、响应、mock 样例、错误码、安全约定和前端接入顺序；同步文件地图、微信云开发落地清单、后续任务清单、当前状态、本地与云端数据边界和任务启动卡。
- 验证：文档检查通过；本次未改 APP 代码。

## 2026-06-10

- 范围：数据服务层 / 云端 port 草案
- 变更：新增 `src/utils/cloudGameDataPort.ts`，预留 `loginOrCreateUser`、`getHomeState`、`saveGameState`、`getLeaderboard` 云函数调用位置；`gameDataService.ts` 重新导出云端 port 工厂，但 `activeGameDataPort` 仍保持本地实现；`test:data-service` 增加云端 port 未启用的边界检查；同步当前状态、数据边界、微信云开发清单、文件地图、验证记录和后续任务清单。
- 验证：`npm.cmd run test:data-service`、`npm.cmd run lint`、`npm.cmd run build`、`npm.cmd run verify:android` 均通过。

## 2026-06-10

- 范围：数据服务层 / 云端化准备
- 变更：将 `src/utils/gameDataService.ts` 升级为异步 `GameDataPort` 边界，`loadGameState()`、`saveGameState()`、`syncLeaderboard()` 当前仍走本地实现；`App.tsx` 改为异步加载和保存游戏状态；更新 `test:data-service` 边界测试，并同步当前状态、数据边界、微信云开发清单、验证记录和后续任务清单。
- 验证：`npm.cmd run test:data-service`、`npm.cmd run lint`、`npm.cmd run build`、`npm.cmd run test:browser:dist`、`npm.cmd run verify:android` 均通过。

## 2026-06-10

- 范围：安卓内测 / v0.1.0 发包基线
- 变更：新增 `v0.1.0安卓内测发包基线.md`，记录已通过真机验证的发包信息、HBuilder X 入口、图标启动页路径、打包前检查、真机冒烟流程和下一版版本号建议；更新安卓打包清单、当前状态、文件地图、任务启动卡和后续任务清单。
- 验证：文档检查通过；本次未改 APP 代码。

## 2026-06-10

- 范围：HBuilder X / 图标与启动页资产
- 变更：导入用户提供的启动页和图标原图到 `public/branding`，生成 `splash-1080x1920.png`、`app-icon-1024.png` 和 `app-icon-512.png`；更新 README、安卓打包清单、当前状态和文件地图；`test:android-package` 新增品牌图片 PNG 尺寸检查。
- 验证：`npm.cmd run verify:android` 与 `npm.cmd run lint` 均通过；`test:android-package` 输出 `brandingAssetsReady: true`；2026-06-10 用户反馈 HBuilder X 打包后的安卓 APK 真机通过。

## 2026-06-09

- 范围：HBuilder X / 安卓内测版本与打包清单
- 变更：将 `package.json` 版本升为 `0.1.0`，README 标记当前版本为“安卓内测 v0.1.0”；新增 `安卓打包清单.md`，记录 `verify:android`、HBuilder X `dist` 入口、版本命名、图标启动页、权限最小化、白屏诊断和发包记录模板；文件地图和当前状态已接入该清单。
- 验证：`npm.cmd run verify:android`、`npm.cmd run lint` 均通过；命令输出已显示包版本为 `kids-brain-game-park@0.1.0`。

## 2026-06-09

- 范围：HBuilder X / 安卓打包一键验证
- 变更：新增 `npm run verify:android`，将 `build`、`test:android-package`、`test:browser:dist` 收口为 HBuilder X 打包前的一条命令；更新 README、`HBuilderX打包白屏排查.md` 和当前状态。
- 验证：`npm.cmd run verify:android`、`npm.cmd run lint` 均通过；`verify:android` 已确认产物资源路径、BGM 相对路径和生产 `dist` 浏览器挂载均正常。

## 2026-06-09

- 范围：HBuilder X / 生产 dist 烟测
- 变更：新增 `tests/dist-browser-smoke.mjs` 和 `npm run test:browser:dist`，使用本地静态服务器加载构建后的 `dist`，检查 React 已挂载、启动诊断面板未出现、关键首页元素存在、资源没有 404、页面没有运行时错误；更新 README 和 `HBuilderX打包白屏排查.md`，把该命令加入 APK 打包前检查流程。
- 验证：`npm.cmd run build`、`npm.cmd run test:android-package`、`npm.cmd run test:browser:dist`、`npm.cmd run lint` 均通过；`test:browser:dist` 确认生产产物 `appMounted: true`、诊断面板未出现、资源无 404、页面无运行时错误。

## 2026-06-09

- 范围：HBuilder X / 真机白屏诊断
- 变更：在 `index.html` 加入内联启动诊断脚本，捕获资源加载失败、运行时错误、异步错误和 React 未挂载状态，并在页面显示 `app-boot-diagnostics` 面板；`src/main.tsx` 在 React 挂载后写入 `data-app-mounted="true"`，正常启动时不显示诊断；`tests/android-package-smoke.mjs` 增加诊断脚本存在性检查。
- 验证：`npm.cmd run lint`、`npm.cmd run build`、`npm.cmd run test:android-package`、`npm.cmd run test:browser:shortcuts`、`npm.cmd run test:browser:audio` 均通过。

## 2026-06-09

- 范围：HBuilder X / 安卓 APK 打包前检查
- 变更：新增 `tests/android-package-smoke.mjs` 和 `npm run test:android-package`，自动检查 `dist/index.html` 是否使用 `./assets/...` 相对路径、引用资源是否真实存在、BGM 是否以 `./audio/Sunlight_on_the_Sandbox.mp3` 打包；更新 README 和 `HBuilderX打包白屏排查.md`，明确 HBuilder X 应使用构建后的 `dist` 目录。
- 验证：`npm.cmd run build`、`npm.cmd run test:android-package`、`npm.cmd run lint` 均通过；`test:android-package` 输出 `androidPackageReady: true`，检查到 6 个相对资源并确认 BGM 为 `./audio/Sunlight_on_the_Sandbox.mp3`。

## 2026-06-09

- 范围：HBuilder X / 安卓 APK 白屏修复
- 变更：在 `vite.config.ts` 设置 `base: './'`，避免 Vite 产物在 HBuilder X 安卓 WebView 中生成 `/assets/...` 根路径资源导致白屏；将 `src/utils/audio.ts` 的 BGM 路径改为基于 `import.meta.env.BASE_URL`；新增 `src/vite-env.d.ts`；新增 `HBuilderX打包白屏排查.md` 记录打包前检查项。
- 验证：`npm.cmd run lint`、`npm.cmd run build`、`npm.cmd run test:browser:audio`、`npm.cmd run test:browser:shortcuts`、`npm.cmd run test:browser:points` 均通过；`dist/index.html` 已生成 `./assets/...` 相对路径。

## 2026-06-09

- 范围：小程序迁移准备 / 数据服务层
- 变更：新增 `src/utils/gameDataService.ts`，暴露 `loadGameState()`、`saveGameState()`、`syncLeaderboard()`；新增 `src/utils/leaderboardService.ts`，将排行榜默认数据、用户榜单合并、读写同步从 `gameHelpers.ts` 抽出；新增 `tests/data-service-boundary.test.mjs` 与 `npm run test:data-service`，防止排行榜/存储逻辑回流到 `gameHelpers.ts`；新增 `本地与云端数据边界.md` 记录本地数据、云端数据和迁移入口。
- 验证：`npm run test:data-service`、`npm run test:storage`、`npm run test:points`、`npm run test:browser:points`、`npm run test:browser:shortcuts`、`npm run lint`、`npm run build` 均通过。

## 2026-06-09

- 范围：品牌命名 / 可见 APP 名称
- 变更：将界面顶部、桌面说明卡、加载文案、网页标题和 README 主标题中的可见品牌从「好奇小熊」/「儿童萌趣益智游戏乐园」调整为「小布智趣岛」，顶部副标题改为「Lv.8 探索馆」；项目目录和知识库目录暂不改名，避免破坏现有路径和交接入口。
- 验证：`npm run lint`、`npm run build`、`npm run test:browser:shortcuts` 均通过。

## 2026-06-09

- 范围：小程序迁移准备 / 存储适配层
- 变更：新增 `src/utils/gameStorage.ts`，统一封装游戏存档、排行榜、签到、成就、音频设置和自定义头像缓存的读写；`gameHelpers.ts`、`audio.ts`、`BombGame.tsx`、`ProfileView.tsx` 不再直接调用 `localStorage`；新增 `tests/storage-adapter.test.mjs` 与 `npm run test:storage`，防止源码绕过适配层。
- 验证：`npm run test:storage`、`npm run test:points`、`npm run lint`、`npm run build`、`npm run test:browser:points`、`npm run test:browser:games`、`npm run test:browser:audio`、`impeccable detect --json src` 均通过；`npm run test:browser:audio` 并行时曾出现一次 `page.goto` 超时，单独重跑通过。

## 2026-06-09

- 范围：五子棋 / 规则与回归测试
- 变更：新增 `src/utils/gomokuRules.ts`，将五子棋空棋盘、胜利和平局判断抽为可测试工具；`GomokuGame.tsx` 接入共享规则并增加单局结算保护，避免游戏结束后重复加分；新增 `tests/gomoku-rules.test.mjs` 与 `npm run test:gomoku`；扩展 `tests/games-browser-smoke.mjs`，覆盖五子棋 AI 模式玩家胜利 `+100` 和结束后点击不重复加分。
- 验证：`npm run test:gomoku`、`npm run test:browser:games`、`npm run test:browser:points`、`npm run test:browser:shortcuts`、`npm run test:browser:audio`、`npm run test:points`、`npm run lint`、`npm run build`、`impeccable detect --json src` 均通过。

## 2026-06-09

- 范围：UI / 奖励弹窗绘本化
- 变更：将成就解锁弹窗和签到奖励弹窗从厚重高饱和弹窗改为“绘本奖励页”视觉：统一纸张质感、山丘场景、贴纸宝盒、轻量边框和 `kid-game-primary` 主按钮；保留 `#achievement-alert-root` 与 `#reward-claimed-modal`，兼容现有浏览器回归脚本。
- 验证：`npm run lint`、`npm run build`、`npm run test:points`、`npm run test:browser:points`、`npm run test:browser:shortcuts`、`npm run test:browser:games`、`npm run test:browser:audio`、`impeccable detect --json src` 均通过；Playwright 检查移动端签到奖励弹窗尺寸约 384x422，按钮完整可见。

## 2026-06-09

- 范围：音频 / BGM 与音效控制
- 变更：将顶部声音入口拆为背景音乐和音效两个独立按钮；新增 `kids_applet_music_muted` 与 `kids_applet_effects_muted` 独立本地状态；BGM 增加淡入、淡出和播放失败兜底；新增 `tests/audio-controls-browser-smoke.mjs` 与 `npm run test:browser:audio`。
- 验证：`npm run lint`、`npm run build`、`npm run test:browser:audio`、`npm run test:points`、`npm run test:browser:points`、`npm run test:browser:shortcuts`、`npm run test:browser:games`、`impeccable detect --json src` 均通过；Playwright 检查顶部两个音频按钮尺寸均为 28x28，未溢出顶部栏。

## 2026-06-09

- 范围：音频 / 背景 BGM
- 变更：将用户提供的 `Sunlight_on_the_Sandbox.mp3` 复制到 `public/audio/`；`soundSynth.startBgm()` 改为循环播放 `/audio/Sunlight_on_the_Sandbox.mp3`，保留现有点击、移动、得分、胜利、警告、爆炸等合成音效；清理旧合成 BGM 分支。
- 验证：`http://localhost:3000/audio/Sunlight_on_the_Sandbox.mp3` 返回 200 `audio/mpeg`，文件大小 3535116；`dist/audio/Sunlight_on_the_Sandbox.mp3` 已生成；`npm run lint`、`npm run build`、`npm run test:browser:shortcuts`、`npm run test:points`、`npm run test:browser:points`、`npm run test:browser:games` 均通过。

## 2026-06-09

- 范围：UI / 首页快捷入口
- 变更：将首页绘本卡片中的 `背包补给`、`今日路线`、`成长印章` 从静态装饰改为真实按钮；背包补给滚动到背包/商店区域，今日路线切到游戏堡，成长印章切到荣誉墙；新增 `tests/home-shortcuts-browser-smoke.mjs` 和 `npm run test:browser:shortcuts`。
- 验证：`npm run test:browser:shortcuts` 输出三项均为 `true`；in-app browser 已验证背包补给在内部滚动容器中正确定位；`npm run lint`、`npm run test:points`、`npm run test:browser:points`、`npm run test:browser:games`、`npm run build`、`impeccable detect --json src` 均通过。

## 2026-06-09

- 范围：测试 / 分支结算浏览器回归
- 变更：扩展 `tests/games-browser-smoke.mjs`，新增数字炸弹 AI 踩雷胜利、五子棋亲子模式白子胜利、成就领奖防重复验证；随机数恢复和棋子数量等待改为脚本内稳定工具。
- 验证：`npm run test:browser:games` 输出数字炸弹惩罚鼓励 `+10`、数字炸弹 AI 胜利 `+50`、数字华容道 `+80`、舒尔特 `+60`、五子棋黑子胜利 `+50`、五子棋白子胜利 `+50`、成就领奖首领 `+50` 且第二次 `+0`；`npm run test:points`、`npm run test:browser:points`、`npm run lint`、`npm run build`、`impeccable detect --json src` 均通过。

## 2026-06-09

- 范围：测试 / 四个游戏浏览器回归
- 变更：新增 `tests/games-browser-smoke.mjs` 和 `npm run test:browser:games`，覆盖数字炸弹惩罚鼓励、数字华容道 3x3 解谜通关、舒尔特 3x3 顺序点击通关、五子棋亲子模式黑子五连胜利；更新知识库验证状态和后续任务清单。
- 验证：`npm run test:browser:games` 输出数字炸弹 `+10`、数字华容道 `+80`、舒尔特 `+60`、五子棋 `+50`；`npm run test:points`、`npm run test:browser:points`、`npm run lint`、`npm run build`、`impeccable detect --json src` 均通过。

## 2026-06-09

- 范围：UI / 方案 B 游戏内页章节化
- 变更：四个游戏内页新增绘本章节场景条；数字炸弹、数字华容道、舒尔特训练、五子棋分别接入独立章节文案、场景底色和小插图符号；`DESIGN.md` 补充游戏章节场景组件规则。
- 验证：`npm run lint`、`npm run test:points`、`npm run test:browser:points`、`npm run build` 通过；`impeccable detect --json src` 无命中；已用 Playwright 截图抽检移动端四个游戏入口页。

## 2026-06-09

- 范围：UI / 方案 B 绘本探索乐园
- 变更：更新 `DESIGN.md` 的视觉方向为 `storybook exploration park`；新增绘本纸张、故事场景、章节卡、路线虚线等共享样式；首页改为绘本场景和今日章节；游戏入口由 2x2 卡片改为四个故事章节列表。
- 验证：`npm run lint`、`npm run test:points`、`npm run test:browser:points`、`npm run build` 通过；`impeccable detect --json src` 无命中；已用 Playwright 截图抽检移动端首页和游戏章节页。

## 2026-06-09

- 范围：UI / 外围成长系统
- 变更：优化 `AchievementsView.tsx`、`ProfileView.tsx`、`DailyCheckIn.tsx`；荣誉墙、个人档案、记录卡和签到奖励弹窗移除厚底边框、强阴影和 bounce 动效，接入共享轻量卡片体系。
- 验证：`npm run lint`、`npm run test:points`、`npm run test:browser:points`、`npm run build` 通过；`impeccable detect --json src` 无命中；已用 Playwright 截图抽检移动端个人档案和荣誉墙。

## 2026-06-09

- 范围：UI / 游戏内页设计系统
- 变更：新增 `DESIGN.md`；补充 `kid-game-panel`、`kid-game-header`、`kid-game-note`、`kid-game-primary`、`kid-game-result` 共享样式；统一数字炸弹、数字华容道、舒尔特训练、五子棋的外层容器、说明卡、结果卡和主按钮；保留棋盘和游戏物件的必要触感。
- 验证：`npm run lint`、`npm run test:points`、`npm run test:browser:points`、`npm run build` 通过；四个游戏组件 `impeccable detect` 无命中；已用 Playwright 截图抽检四个游戏移动端入口页。

## 2026-06-09

- 范围：UI / taste + impeccable
- 变更：新增 `PRODUCT.md` 和 `.impeccable/live/config.json`；首屏不再首次进入就弹成就弹窗；桌面说明卡、游戏入口、排行榜和成就弹窗信息块做降噪；替换部分 bounce 动画并补充 reduced-motion 降级。
- 验证：`npm run lint`、`npm run test:points`、`npm run test:browser:points`、`npm run build` 通过；`impeccable detect src/App.tsx` 无命中；已用 Playwright 截图检查移动端首页、游戏入口、排行榜和桌面首页。

## 2026-06-09

- 范围：知识库 / 测试与合规
- 变更：新增 `儿童隐私合规清单.md` 和 `四个游戏测试脚本.md`；修正 `问题与风险台账.md` 的构建验证标题；更新归档长文的新窗口读取口径。
- 验证：已接入 README、当前状态、任务启动卡、后续任务清单和新窗口提示词。

## 2026-06-09

- 范围：知识库 / 小程序规划
- 变更：新增 `小程序路线决策矩阵.md` 和 `微信云开发落地清单.md`；将 `验证记录.md` 压缩为当前摘要，新增 `验证归档.md` 保存历史输出。
- 验证：已更新 README、当前状态、任务启动卡、决策记录和任务清单，确保新文档可从入口按需读取。

## 2026-06-09

- 范围：知识库
- 变更：新增 `任务启动卡.md`，并更新 `README.md`、`新窗口提示词.md`、`项目速读.md`、`文件地图.md`、`小程序迁移方案.md`、`当前状态.md`、`后续任务清单.md` 中的过期状态。
- 验证：已清理高频入口里的旧结论，避免新窗口把积分重复发放、依赖未安装、模板残留或构建大 chunk 警告误认为当前未处理问题。

## 2026-06-09

- 范围：知识库
- 变更：新增 `当前状态.md`、`项目速读.md`、`文件地图.md`、`问题与风险台账.md`、`决策记录.md`，并更新 `README.md` 与 `新窗口提示词.md`。
- 验证：已读取新增文件和索引，确认路径与中文内容正常。

## 2026-06-09

- 范围：知识库
- 变更：新增 `变更日志.md`、`验证记录.md`、`小程序迁移方案.md`；将 `项目概览与交接.md` 标为完整背景归档文档。
- 验证：已读取新增文件、Markdown 文件列表和关键内容，确认写入正常。

## 2026-06-09

- 范围：版本回溯
- 变更：在 `D:\儿童微信小程序开发` 初始化本地 Git 仓库，新增根目录 `.gitignore` 和 `版本回溯说明.md`，创建初始 checkpoint `6c9ea90`。
- 验证：Git 初始化和初始提交成功。

## 2026-06-09

- 范围：版本回溯
- 变更：给当前稳定状态创建 Git tag：`baseline-2026-06-09`。
- 验证：tag 创建成功，待提交本次知识库记录。

## 2026-06-09

- 范围：APP 修复
- 变更：修复四个游戏组件积分重复发放；新增 `tests/points-system.test.mjs` 和 `npm run test:points`；安装依赖并生成 `package-lock.json`。
- 验证：`npm run test:points`、`npm run lint`、`npm run build` 均通过；`http://localhost:3000` 返回 200；构建存在大 chunk 警告。

## 2026-06-09

- 范围：APP 回归测试
- 变更：新增 Playwright devDependency、`tests/points-browser-smoke.mjs` 和 `npm run test:browser:points`。
- 验证：浏览器回归显示初始积分 100，打卡后 120，购买糖果后 105；`npm run test:browser:points`、`npm run test:points`、`npm run lint`、`npm run build` 均通过。

## 2026-06-09

- 范围：APP 元数据清理
- 变更：清理 AI Studio 模板残留；更新包名、README、HTML title、metadata、env 示例；卸载未使用的 Gemini/Express/Dotenv 相关依赖。
- 验证：模板残留搜索无命中；`npm run test:points`、`npm run test:browser:points`、`npm run lint`、`npm run build` 均通过。

## 2026-06-09

- 范围：APP 构建优化
- 变更：为 Vite 增加 `manualChunks`；将 `AchievementsView.tsx` 从全量 `lucide-react` 导入改为显式图标映射。
- 验证：`npm run test:points`、`npm run test:browser:points`、`npm run lint`、`npm run build` 均通过；大 chunk 警告已消失。

## 2026-06-09

- 范围：知识库/产品架构
- 变更：新增 `数据模型草案.md`，覆盖用户、积分流水、游戏记录、成就、打卡、背包、排行榜和云函数/API 草案。
- 验证：文档已接入 README、当前状态、决策记录和任务清单。

## 2026-06-08

- 范围：知识库
- 变更：创建 Obsidian 知识库基础结构，新增 `README.md`、`项目概览与交接.md`、`后续任务清单.md`。
- 验证：已读取文件列表和内容，确认写入正常。

## 2026-06-08

- 范围：APP 分析
- 变更：完成初步源码分析，未修改 APP 源码。
- 验证：尝试运行 `npm run lint`，因 `tsc` 不可用失败。

## 2026-06-10

- 范围：微信云开发 / `getHomeState` 实现草案
- 变更：新增 `cloudfunctions/getHomeState/index.cjs`，实现用户档案、统计、成就、背包、签到和排行榜快照到前端 `GameState` 的读取映射；新增 `tests/get-home-state-cloudfunction.test.mjs`，并将其纳入 `npm run test:data-service`；更新 `cloudfunctions/getHomeState/README.md` 和云函数骨架测试，标记该函数已从 planning skeleton 升级为 implementation draft。
- 验证：`npm.cmd run test:data-service`、`npm.cmd run lint`、`npm.cmd run verify:android` 均已通过。

## 2026-06-10

- 范围：微信云开发 / `loginOrCreateUser` 实现草案
- 变更：新增 `cloudfunctions/loginOrCreateUser/index.cjs`，实现 `_openid` 校验、用户查找/创建、默认统计、默认背包和默认成就初始化；新增 `tests/login-or-create-user-cloudfunction.test.mjs`，并将其纳入 `npm run test:data-service`；更新 `cloudfunctions/loginOrCreateUser/README.md` 和云函数骨架测试，标记该函数已从 planning skeleton 升级为 implementation draft。
- 验证：`npm.cmd run test:data-service`、`npm.cmd run lint`、`npm.cmd run verify:android` 均已通过。
